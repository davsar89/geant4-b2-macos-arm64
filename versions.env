# Pinned inputs of the portable build (sourced by build_mac.sh; its hash keys the CI dependency cache).
G4_VERSION=11.4.2
QT_VERSION=6.9.3          # newest Qt that still supports macOS 12
XERCES_VERSION=3.3.0
MACOS_MIN=12.0            # deployment target (Qt 6.9 minimum)
DEPS_REV=1                # bump after changing how build_mac.sh builds the dependencies
